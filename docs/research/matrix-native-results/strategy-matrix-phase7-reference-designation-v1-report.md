# STRATEGY_MATRIX_PHASE7_REFERENCE_DESIGNATION_R1 — report

Status: COMPLETE — research-reference designation only, no new evidence generated ｜ 2026-08-17

Designates **Constructor E** (`GREEDY_MINMAX_THEN_SUM_OVERLAP_V1`) as the
next default *research* comparator for future in-scope Matrix constructor
studies, strictly within the scope the source synthesis actually tested.
Neutral aliases (Method E / Reference R / Structure A/B/C) are used as the
primary label throughout this report per the Owner packet's
data-minimization instruction; §0 gives the one-time identity crosswalk.

This is a **research reference designation only**. It changes no runtime,
product, or default behavior; it is not a prediction method, a runtime
strategy, a profitability claim, or a global-optimum claim.
`RUNTIME_PROMOTION` is fixed at `NOT_AUTHORIZED` (§4).

Reads exactly one canonical source — the already-SEALED Phase-7
cross-structure synthesis — pinned to the commit that introduced it. No new
combinatorial enumeration, no portfolio regeneration, no change to
Constructor E, no parameter tuning, no new matrix-native-result cell, no
alteration of any prior sealed artifact, and no outcome/history data read
(§7). Full normalized record:
`strategy-matrix-phase7-reference-designation-v1.json` (generated by
`tools/generate_strategy_matrix_phase7_reference_designation.py`).
Independently re-derived in
`tests/unit/test_strategy_matrix_phase7_reference_designation.py` directly
from the pinned source commit, not trusted from the generator's own output.

## 0. Identity crosswalk

```text
METHOD E (designated):  GREEDY_MINMAX_THEN_SUM_OVERLAP_V1
REFERENCE R (prior):    Arm-B, GREEDY_MIN_OVERLAP_CONSTRUCTOR_{*}_V1
                         (the Phase-5 non-Sidon low-overlap greedy
                         constructor; standing reference going into Phase 7;
                         UNCHANGED and UNREPLACED outside this designation's
                         effective scope — see §2/§3)

Structure A  = BIG_LOTTO (B649)
Structure B  = DAILY_539 (T539)
Structure C  = POWER_LOTTO Zone-1 (P638)

SOURCE SYNTHESIS_ID:  STRATEGY_MATRIX_PHASE7_NEXT_GENERATION_CONSTRUCTOR_CROSS_STRUCTURE_SYNTHESIS_R1
SOURCE COMMIT:        e7e886ff0537487f1ea2cbac848ea93382a22f43
SOURCE PATH:          docs/research/matrix-native-results/strategy-matrix-phase7-next-generation-constructor-cross-structure-synthesis-v1-result.json
```

`e7e886f` is verified (`git log --follow`) to be that file's *only* commit —
the later shallow-clone-safety fix (`c984bbd`) touched only the generator
and test scripts that produced it, never the sealed data itself. This
designation task's own working commit sits on canonical `origin/main`, a
descendant of the PR #145 merge (`343f78f`) that published the source
synthesis.

## 1. What the source synthesis established (verified verbatim, not rederived)

```text
CROSS_STRUCTURE_CLASSIFICATION:   NEXT_GEN_CONSTRUCTOR_SUPPORTED_IN_3_NATIVE_STRUCTURES
REFERENCE_PROMOTION_DECISION:     PROMOTE_TO_NEXT_REFERENCE_CONSTRUCTOR
GLOBAL_OPTIMUM_STATUS:            UNKNOWN
```

These three fields, plus `constructor_id`, `ladder`, `q2_e_ge_reference_every_k_gt_1`,
`q3_e_gt_reference_at_k_10_15_20`, and the k∈{1,3,5} tie confirmation in
`q5_improvement_patterns`, are checked against exact expected values before
this designation is built; any mismatch raises
`STOP_REFERENCE_DESIGNATION_AUTHORITY_UNRESOLVED` rather than silently
proceeding on a changed premise. This task did not rerun, resample, or
second-guess any of them — it only reads and gates on them.

## 2. Effective scope — where Method E is the next research reference

Method E is the default *research* comparator for future in-scope Matrix
constructor studies only where **all** of the following hold simultaneously:

```text
1. PRIMARY_TESTED_COVERAGE_EVENT   -- the event the source actually tested
                                       (M3_PLUS / ZONE1_M3_PLUS, per the
                                       source's own recommendation_scope)
2. K_GTE_10                        -- within the tested ladder: k in {10,15,20}
3. DOMAIN_COMPATIBLE_WITH_SEALED_EVIDENCE
                                    -- BIG_LOTTO, DAILY_539, or POWER_LOTTO
                                       Zone-1 (the 3 sealed native structures)
4. METHOD_E_APPLICABLE_UNCHANGED   -- GREEDY_MINMAX_THEN_SUM_OVERLAP_V1
                                       applied exactly as sealed, no
                                       parameter changes
```

This mirrors the source's own `recommendation_scope` exactly: strict gain
over Reference R in all three structures at k=10,15,20, within the tested
k≤20 ladder. Nothing here widens that scope.

## 3. Excluded scope — where this designation establishes nothing

```text
k IN {1,3,5}:            TIE_ONLY_DOES_NOT_TRIGGER_REPLACEMENT
                          Method E exactly ties Reference R (never worse) at
                          every one of these k, in all three structures. A
                          tie alone is not grounds to replace Reference R —
                          Reference R is NOT superseded for k in {1,3,5} by
                          this designation.
k > 20:                   NOT_ESTABLISHED_BY_THIS_DESIGNATION
P638 Zone-2:              NOT_ESTABLISHED_BY_THIS_DESIGNATION
Any 4th structure:        NOT_ESTABLISHED_BY_THIS_DESIGNATION
Predictive advantage:     NOT_ESTABLISHED_BY_THIS_DESIGNATION
Profitability:            NOT_ESTABLISHED_BY_THIS_DESIGNATION
Prize economic value:     NOT_ESTABLISHED_BY_THIS_DESIGNATION
Incompatible domains:     NOT_ESTABLISHED_BY_THIS_DESIGNATION
```

Extrapolation policy: `DO_NOT_EXTRAPOLATE`. Untested is reported as
untested, not as a plausible generalization.

## 4. Designation status, prior-reference treatment, and claim boundary

```text
REFERENCE_DESIGNATION_STATUS:  METHOD_E_IS_NEXT_RESEARCH_REFERENCE_IN_SCOPE
RUNTIME_PROMOTION:             NOT_AUTHORIZED
GLOBAL_OPTIMUM_STATUS:         UNKNOWN
```

**Prior reference treatment** — Reference R remains the historical/sealed
comparator for every already-sealed result that used it. This designation
does not alter, invalidate, or retroactively re-grade any prior sealed
artifact.

**Semantics** — reference designation means: the default scientific
comparator for future in-scope Matrix constructor studies; Reference R
remains the historical/sealed comparator; future in-scope candidates should
compare against Method E. It does **not** mean: a production default, a
prediction method, a runtime strategy, a profitability claim, universal
portability, or a global optimum.

```text
CLAIM_BOUNDARY:
  production_default:      NOT_CLAIMED
  prediction_method:       NOT_CLAIMED
  runtime_strategy:        NOT_CLAIMED
  profitability_claim:     NOT_CLAIMED
  universal_portability:   NOT_CLAIMED
  global_optimum:          NOT_CLAIMED
```

## 5. What this designation does not do

```text
RERAN_A_B_C:                        NO
REGENERATED_PORTFOLIOS:             NO
CHANGED_METHOD_E:                   NO
TUNED_PARAMETERS:                   NO
NEW_MATRIX_NATIVE_RESULT_CELL:      NO
ALTERED_PRIOR_SEALED_ARTIFACTS:     NO
INSPECTED_OUTCOME_OR_HISTORY_DATA:  NO
PUBLISHED_OR_PUSHED:                NO
PROMOTED_RUNTIME_BEHAVIOR:          NO
```

## 6. Repository state note (not part of the scientific finding, disclosed for completeness)

Local `main` at the time this task was commissioned had diverged from
canonical `origin/main` before the PR #145 merge (`343f78f`) that published
the source synthesis, re-deriving equivalent research independently under
different commit hashes. This designation task was executed in an isolated
worktree branched fresh from `origin/main` specifically to avoid that
divergent history — local `main` and its own in-progress work were left
completely untouched by this task.

## No-rescue statement

The source synthesis's classification, promotion recommendation, and
global-optimum status were fixed before this task read the file — this task
only gates on them and derives a designation record; it does not compute,
tune, or choose any of the three required values. The k∈{1,3,5} tie was not
smoothed into a full replacement of Reference R — it is reported as
excluded scope precisely because the source only supports it as a tie.

## Artifacts

```text
tools/generate_strategy_matrix_phase7_reference_designation.py
docs/research/matrix-native-results/strategy-matrix-phase7-reference-designation-v1.json
docs/research/matrix-native-results/strategy-matrix-phase7-reference-designation-v1-report.md   (this file)
tests/unit/test_strategy_matrix_phase7_reference_designation.py
```

Do not rerun A/B/C, regenerate portfolios, change Method E, tune parameters,
create another native-result cell, alter prior sealed artifacts, or inspect
outcome/history data in this task. Do not push, open a PR, or start
`MATRIX_PHASE7_REFERENCE_DESIGNATION_CANONICALIZE_PUBLISH_R1` in this task.
